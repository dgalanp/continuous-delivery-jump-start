apply plugin: "findbugs"

findbugs {  
  ignoreFailures = true
  toolVersion = "2.0.1"
}

tasks.withType(FindBugs) {    
  reports {        
    xml.enabled = false       
    html.enabled = true  
  }
}